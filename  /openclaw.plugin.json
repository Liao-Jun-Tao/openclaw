{
  "id": "claude-code",
  "name": "Claude Code Compatibility",
  "description": "Claude Code CLI tools and commands for OpenClaw - file editing, bash execution, grep search, glob matching, and more.",
  "enabledByDefault": true,
  "skills": ["./skills", "./commands"],
  "uiHints": {
    "sandboxMode": {
      "label": "Sandbox Mode",
      "help": "Run bash commands in isolated sandbox environment"
    },
    "allowedCommands": {
      "label": "Allowed Commands",
      "help": "Comma-separated list of allowed bash commands (empty = all allowed)"
    },
    "readonlyMode": {
      "label": "Read-Only Mode",
      "help": "Disable file editing tools - read-only access"
    }
  },
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "sandboxMode": {
        "type": "boolean",
        "default": false
      },
      "allowedCommands": {
        "type": "array",
        "items": { "type": "string" },
        "default": []
      },
      "readonlyMode": {
        "type": "boolean",
        "default": false
      },
      "maxBashTimeoutMs": {
        "type": "integer",
        "minimum": 1000,
        "maximum": 600000,
        "default": 300000
      }
    }
  }
}
